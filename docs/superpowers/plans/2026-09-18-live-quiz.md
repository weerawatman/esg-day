# Data Guardians live quiz revision

User requests a Kahoot-like experience, ten questions, three randomly assigned teams, countdowns and more engaging play. Implement directly within the existing approved game.

- [ ] Preserve v1 room rules and results; snapshot v2 rules into new rooms.
- [ ] Add tests for three balanced random teams (20 => 7/7/6), ten questions, selectable timer, 3-second intro, answer deadline, leaderboard phase, hidden answers, final ranking and legacy room compatibility.
- [ ] Implement server rules and settings. Keep server-authoritative times and scores; bonus final two questions; no speed bonus. Score normalization uses each room's actual question total and team size.
- [ ] Build expressive live quiz UI: purple stage, four colored shape choices, large countdown ring and progress bar, 3-2-1 intro, team reveal after nickname sign-in, answer histogram, separate animated leaderboard, winner confetti, opt-in synthesized sound with mute.
- [ ] Verify full ten-question flow with 20 sessions, desktop/mobile screenshots, round transitions, refresh and legacy result view. Update README and leave local server running.

User clarified employee ID + nickname. Validate unique normalized employee IDs per room; expose only nicknames publicly. This identifies game participants without claiming corporate authentication. Persist an unguessable request token before joining so a lost response can be retried safely.

Colors: deep violet #251454, electric purple #7651E8, orange #F4A340, red #DA4056, blue #267DC4, teal #158776. Keep color plus shape/text labels. Respect reduced motion; audio starts only after a user gesture.

Tests: `npm test`, `npm run test:browser`. New timing tests use an injected clock; browser tests exercise real 3-second intro and countdown updates.
